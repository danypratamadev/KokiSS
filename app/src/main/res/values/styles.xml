<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimary">@color/WindowBg</item>
        <item name="colorPrimaryDark">@color/WindowBgOver</item>
        <item name="colorAccent">@color/WindowBgActive</item>
    </style>

    <style name="Splashscreen" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorAccent">@color/White</item>
    </style>

    <style name="TabItemStyle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title.Inverse">
        <item name="android:fontFamily">@font/fengardoneue_regular</item>
        <item name="android:textSize">9dp</item>
    </style>

    <style name="spinner_style">
        <item name="android:paddingLeft">7dp</item>
        <item name="android:showDividers">none</item>
        <item name="android:dividerHeight">0dp</item>
        <item name="android:scrollbarAlwaysDrawVerticalTrack">true</item>
        <item name="android:dropDownSelector">@android:color/white</item>
    </style>

</resources>
